#include <iostream> 
using namespace std; 

int main() {
    float length, convLength; 
    string currentUnit, targetUnit; 

    cout << "Enter length: "; 
    cin >> length; 


    cout << "Enter current unit: "; 
    cin >> currentUnit;

    cout << "Enter target unit: ";
    cin >> targetUnit; 

    cout << "New converted length in cm: "  << convLength; 
}